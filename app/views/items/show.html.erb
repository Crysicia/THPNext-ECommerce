<h1>Page produit : <%= @item.name %></h1>

<table>
  <tr>
    <td>
      <em> <%= @item.description %><br> </em>
    </td>
    <td>
      <%= @item.original_price.round(2) %> €<br>
    </td>
    <td>
      <img src="<%=@item.photo%>"><br><br>
    </td>
    <td>
    <% if user_signed_in? %>
      <h2><%= link_to 'Ajouter au panier', cart_path(item: @item, quantity: 1, type: 'increment'), method: :put %></h2>
    <% else %>
      <%= link_to "s'inscrire ou se connecter pour acheter", new_user_session_path %> 
    <% end %>
    </td>
  </tr>
</table>