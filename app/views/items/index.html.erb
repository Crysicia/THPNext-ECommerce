<h1>Greenie : tous nos produits</h1>

<table>

<% @items.each do |item| %>
  <tr>
    <td>
      <%= link_to item.name, item_path(item) %>
    </td>
    <td>
      <em> <%= item.description %><br> </em>
    </td>
    <td>
      <%= item.original_price.round(2) %> €<br>
    </td>
    <td>
      <img src="<%=item.photo%>"><br><br>
    </td>
  </tr>
<% end %>

</table>
